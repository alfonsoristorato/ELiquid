@model IEnumerable<ELiquid.Data.Models.ElecLiquid>

@{
    ViewBag.Title = "Index";


}

<h2>E-Liquids</h2>
<div class="row">
    <p class="col-sm-3">
        @Html.ActionLink("Add New", "Create", "", new { @class = "btn home-buttons" })
    </p>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        
            @*foreach (var item in Model)
            {
            @Html.Partial("_Category", item)
            break;
            }*@
            

        <p class="search col-sm-9">
            @Html.TextBox("Search", "", new { @class = "custom-form" })
            <input type="submit" value="Filter" class="btn home-buttons" />
        </p>
        }

    </div>

@foreach (var item in Model)
{
    @Html.Partial("_ElecLiquidsSummary", item)
}


@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PGVGRatio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PGVGRatio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@
@*@section footer{
    <div>This is a custom footer</div>
    }*@
@section Scripts{
    @Scripts.Render("~/Scripts/picUpload.js")
    @Scripts.Render("~/bundles/jqueryval")

}